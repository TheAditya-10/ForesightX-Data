# Data Utils

Utility code for service-local concerns lives here.

Current contents focus on configuration so the service can run cleanly in:

- local development
- Docker Compose
- future deployment environments
